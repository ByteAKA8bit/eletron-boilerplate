import React from 'react'
import ReactDOM from 'react-dom/client'
import OtherWindow from '.'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <OtherWindow />
  </React.StrictMode>
)
